<p id="notice"><%= notice %></p>
<h3>Weeks</h3>

<ul>
    <% @weeks.each do |week| %>
        <li><%= link_to week.start_day, week_path(week) %></li>
    <% end %>
    <%= button_to 'Update data', action: 'update_data' %>
</ul>